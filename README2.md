# 장바구니 풀스택 미션
## 요구사항
### 장바구니 2단계 - FE
### 기능 요구 사항
- 장바구니 API를 호출하여 장바구니 상품 데이터를 불러온다.
- 불러온 데이터를 기반으로 클라이언트 상태를 구성하고 관리한다.
- 상품의 선택 여부, 결제 금액, 배송비 등의 상태를 관리한다.
- 상품 선택 여부는 새로고침 시에도 유지되도록 한다.
- 상품 선택에 따른 결제 금액, 배송비 등의 동적인 변경 사항을 처리한다.
- 진입 시, 전체 선택 되어 있는 것이 디폴트이다.
- 상품 선택/해제 시 결제 금액을 동적으로 변경한다.
- 결제 금액이 10만원 이상일 경우 배송비는 무료이다.
- 장바구니 상품의 수량을 변경할 수 있다. (최대 99개)
- 장바구니에 담긴 상품을 제거할 수 있다.
- 상품명은 최대 100자이다.

### 프로그래밍 요구사항
#### State Management
- React의 상태 관리 훅(useState, useReducer)을 사용하여 상태를 구성하고, 필요 시에 Context API 등을 활용해 해당 상태가 필요한 컴포넌트 트리에 상태를 전달합니다.
- 결제 금액, 배송비 등의 파생 상태(Derived State)를 동적으로 계산한다.
#### Data fetching hook
- 서버 API 통신 결과를 관리할 수 있는 함수를 직접 개발한다.
- 반환값에는 데이터, 로딩 여부, 에러 정보 등이 포함되어야 한다.
- 상용 라이브러리의 인터페이스를 모방하는건 무의미하다. 동작 매커니즘을 이해하고 직접 만들어보는게 훨씬 중요하다.
"어떻게 SSOT 지켰을까?", "어떻게 캐싱을 할까?", etc...
#### API
- Step1 에서 만든 API 를 그대로 적용한다.
- 비동기 작업(API 요청)의 상태를 관리하고, 비동기 요청 상태에 따라 적절한 UI를 보여줄 수 있다.
- API 요청 중에는 로딩 상태를 나타내는 UI (예: 텍스트 메시지, 스피너, 스켈레톤 등)를 표시한다.
- API 요청 중 에러가 발생한 경우, 에러 메시지를 사용자에게 알려주는 UI를 표시한다.
#### Test
- Jest, React Testing Library를 사용하여 주요 기능에 대한 테스트 케이스를 작성한다.
- 비동기가 포함된 테스트는 MSW 를 활용한다.
- 주요 기능을 명확히 정의하고, 그것을 어떻게 테스트할 지 스스로 판단한다.
#### 코드 퀄리티
- 컴포넌트와 비즈니스 로직을 분리하여, 재사용 가능하게 한다.
- 재사용 가능하게 하기 위해서, 컴포넌트 내에서 함수를 호출하는 것과, props 를 가지는 방식을 구별하고, 더 나은 방법을 채택한다.
- 사용하기 좋은 인터페이스를 고민한다. 이 컴포넌트의 다양한 use case 를 떠올려본다.
- refetchCart, deleteCartItem 같이 세부 구현을 나타내는 props 가 있다면 이는 이름부터 안티 패턴이다. 다른 use case 에 대응이 어렵다. 1단계 서버 미션에서 다룬, DIP 를 떠올려보자.
#### Library
- 스타일링에는 CSS Module, styled-components, emotion 중 한 가지를 선택하여 사용한다.
- 페이지 전환에 react-router 를 사용하여도 무방하다. (필수 아님)
- 이전에 학습하였거나(msw, storybook), 명시된 라이브러리 외에는 사용하지 않고 직접 구현한다.

## 기능 요구사항 구현 정리
### 사전 고려사항
- 결제 금액, 배송비 : 파생 상태 << 장바구니 데이터와 선택 상태를 기반으로 렌더링마다 계산되게 구현
- 서버에서 가져온 장바구니 데이터와 클라이언트에서만 선택 중인 체크박스 선택상태와 같은것은 localStorage로 동기화
- UI 컴포넌트는 커스텀 훅이 반환하는 액션만 호출하도록 분리
- react-router-dom으로 페이지 라우팅 진행
- 스타일링은 emotion의 styled로 진행
### 인프라 세팅
- msw 세팅으로 백엔드 명세에 맞는 mock 핸들러 작성 필요
- fetch 기반 DI를 반영한 API 호출 인터페이스 및 실구현 함수 필요
### 도메인, 서비스단
- 페칭 커스텀 훅 필요
- api에서 불러온 cartItems를 관리할 useCartService 훅 필요
- localStorage를 이용한 클라이언트의 선택된 아이템 목록 및 수량 기억 필요
- 선택된 아이템과 수량을 기반으로 총 결제 금액과 10만원 이상 시 배송비 무료 로직 필요
### view 단
- 상태와 관계없는 순수 UI 컴포넌트로 만드는 방향
- 헤더, 스켈레톤, 에러 메시지, 주문 확인 버튼
- 장바구니 아이템 컴포넌트
  - 전체선택 체크박스
  - 체크박스
  - 썸네일
  - 상품이름
  - 가격
  - 수량 조절 버튼
  - 삭제 버튼
- 총 결제 금액, 배송비 컴포넌트
  - 안내 메시지(총 주문 금액이 100,000원 이상일 경우 무료 배송됩니다.)
  - 주문 금액
  - 배송비
  - 총 결제 금액
### 라우팅, 서비스와 뷰 조립
- 장바구니 Page : 로딩/에러 상태에 따른 UI 분기, 빈 장바구니 화면 필요
- 주문 확인 Page : 주문 확인 버튼 클릭 시 결제하기 버튼과 총 주문 종류, 총 상품 갯수, 총 결제 금액을 띄워주는 주문 확인 Page
### 테스트
- MSW와 RTL을 연동한 API 로딩, 에러 상태 UI 테스트
- 수량 조절, 삭제 시 결제금액, 배송비가 재계산되는지 테스트
- 페이지를 나갔다가 들어와도 localStorage로 상태가 잘 유지되는지 테스트 필요
